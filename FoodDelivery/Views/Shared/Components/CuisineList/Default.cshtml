@model IEnumerable<CuisineListVM>

@{
    Layout = null;
}

<div class="row mt-3">
    @foreach (var course in Model)
    {
        <div class="col-lg-2 col-md-4 col-6 mb-2">
            <b>@course.Name</b>
        </div>

    }
</div>
